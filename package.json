{
  "name": "carehire",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "NODE_ENV=development node --watch server/index.mjs",
    "check": "node --check server/index.mjs && node --check server/gemini.mjs && node --check public/app.js"
  },
  "dependencies": {
    "@google/genai": ">=2.0.0 <3.0.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0"
  }
}
